//   Variavel de controle; variavel de chacegem; incremento ou decremento
for (let i = 0; i <= 5; i++) {
    //console.log(i)
}

//Verificar se é par

for (let i = 0; i <= 10; i++) {
    const par = i % 2 === 0 ? "par" : "ímpar";
    //console.log(i, par);
}

//percorrer array

const frutas = ["Maçã", "Pêra", "Uva"];

for (let i = 0; i < frutas.length; i++) {
    console.log('\n', i )
    console.log(frutas[i])
}